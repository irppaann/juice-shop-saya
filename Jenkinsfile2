node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
    steps {
      withSonarQubeEnv(installationName: 'SonarQube-Lokal') {
        sh 'docker run --rm --network=host'
      }
    }
  }
}